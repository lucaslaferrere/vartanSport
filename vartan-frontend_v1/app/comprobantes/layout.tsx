'use client';

import AppLayout from '@components/Layouts/AppLayout';

export default function ComprobantesLayout({ children }: { children: React.ReactNode }) {
  return <AppLayout>{children}</AppLayout>;
}
